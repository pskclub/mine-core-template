---
name: mine-core-module
description: Build or extend a feature module in this mine-core service — new module scaffold, new endpoint, new layer, wiring into cmd.Modules, and making one module use another. Use whenever adding a feature, a resource, or a use case, or when an arch import-rule test fails.
---

# mine-core-module

<!-- generated by `make skills` from .claude/skills — do not edit -->

**This is a pointer, not the skill.** The skill is
[.claude/skills/mine-core-module/SKILL.md](../../../.claude/skills/mine-core-module/SKILL.md) — read that file now and follow it.
It is the same instructions Claude Code loads; this copy exists only because
each agent looks for skills in its own directory.
