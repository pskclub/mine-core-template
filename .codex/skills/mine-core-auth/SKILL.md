---
name: mine-core-auth
description: Protect routes and work with the signed-in caller in this mine-core service — AuthRequire/AuthOptional/AuthRole, RegisterAuth wiring, the opaque bearer token scheme, roles, and ownership checks. Use when adding a protected route, changing who may call something, touching the auth module, or debugging a 401/403.
---

# mine-core-auth

<!-- generated by `make skills` from .claude/skills — do not edit -->

**This is a pointer, not the skill.** The skill is
[.claude/skills/mine-core-auth/SKILL.md](../../../.claude/skills/mine-core-auth/SKILL.md) — read that file now and follow it.
It is the same instructions Claude Code loads; this copy exists only because
each agent looks for skills in its own directory.
