---
name: mine-core-workflow
description: Run, build, migrate and ship this mine-core service — make targets, docker compose, live reload, the prisma migration loop, roles, lint and the CI pipeline. Use when starting the service locally, running a migration, debugging a build or a private-module fetch, or checking what CI will do to a change.
---

# mine-core-workflow

<!-- generated by `make skills` from .claude/skills — do not edit -->

**This is a pointer, not the skill.** The skill is
[.claude/skills/mine-core-workflow/SKILL.md](../../../.claude/skills/mine-core-workflow/SKILL.md) — read that file now and follow it.
It is the same instructions Claude Code loads; this copy exists only because
each agent looks for skills in its own directory.
