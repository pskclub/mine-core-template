---
name: mine-core-http
description: Write routes, handlers, request binding, pagination and responses in this mine-core service. Use when adding or changing an HTTP endpoint, binding path/query/body params, returning a list with paging, setting a status code, uploading a file, or regenerating the Postman collection.
---

# mine-core-http

<!-- generated by `make skills` from .claude/skills — do not edit -->

**This is a pointer, not the skill.** The skill is
[.claude/skills/mine-core-http/SKILL.md](../../../.claude/skills/mine-core-http/SKILL.md) — read that file now and follow it.
It is the same instructions Claude Code loads; this copy exists only because
each agent looks for skills in its own directory.
