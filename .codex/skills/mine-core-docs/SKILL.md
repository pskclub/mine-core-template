---
name: mine-core-docs
description: Look up mine-core v2 framework API, behaviour, or documentation. Use when you need the real signature, option, or semantics of anything under github.com/pskclub/mine-core/v2 (core.*, valid.*, repository.*, coretest.*, utils.*, errmsgs.*, mongorepo.*), when a compile error mentions mine-core, when upgrading the mine-core version, or before guessing how a framework helper behaves.
---

# mine-core-docs

<!-- generated by `make skills` from .claude/skills — do not edit -->

**This is a pointer, not the skill.** The skill is
[.claude/skills/mine-core-docs/SKILL.md](../../../.claude/skills/mine-core-docs/SKILL.md) — read that file now and follow it.
It is the same instructions Claude Code loads; this copy exists only because
each agent looks for skills in its own directory.
