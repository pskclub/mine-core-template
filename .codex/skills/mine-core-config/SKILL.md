---
name: mine-core-config
description: Read, add or debug configuration in this mine-core service — .env vs APP_-prefixed OS variables, ENVConfig fields, module-specific keys via ctx.ENV(), opening a new backend in Bootstrap, docker-compose and CI. Use when adding a config key, when a setting is being ignored, or when wiring a new connection (cache, MQ, storage, mongo).
---

# mine-core-config

<!-- generated by `make skills` from .claude/skills — do not edit -->

**This is a pointer, not the skill.** The skill is
[.claude/skills/mine-core-config/SKILL.md](../../../.claude/skills/mine-core-config/SKILL.md) — read that file now and follow it.
It is the same instructions Claude Code loads; this copy exists only because
each agent looks for skills in its own directory.
